#include <stdio.h>

int main()
{
    const char* IDMSG = "Operation Transferred to external Executable.";
    const char* MAINMSG = "Hello World.";

    puts(IDMSG);
    puts(MAINMSG);

    return 0;
}
